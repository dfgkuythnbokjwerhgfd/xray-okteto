apiVersion: v1
kind: Service
metadata:
  name: quait
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "quait"
    port: 8080
  selector:
    app: quait
