apiVersion: v1
kind: Service
metadata:
  name: xrecp
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xrecp"
    port: 8080
  selector:
    app: xrecp
