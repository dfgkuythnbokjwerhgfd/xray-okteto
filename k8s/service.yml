apiVersion: v1
kind: Service
metadata:
  name: liker
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "liker"
    port: 8080
  selector:
    app: liker
