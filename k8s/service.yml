apiVersion: v1
kind: Service
metadata:
  name: catd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "catd"
    port: 8080
  selector:
    app: catd
